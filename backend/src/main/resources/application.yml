spring:
  application:
    name: e-library-system

  datasource:
    url: jdbc:postgresql://db.ynykusfuerewikqnpbrz.supabase.co:5432/postgres
    username: postgres
    password: 6281406276!@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd'T'HH:mm:ss.SSSZ

server:
  port: ${PORT:8088}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.elibrary: INFO
    org.springframework: INFO
